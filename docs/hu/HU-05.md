# HU-05: Publicar Anuncio de Hardware

**Como** vendedor autenticado y verificado  
**Quiero** publicar un anuncio con specs técnicas  
**Para** que compradores puedan encontrarme.

## Endpoint

POST `/api/v1/anuncios`  
🔒 JWT requerido

## Entrada

- titulo (max 100 chars)
- categoria (ENUM)
- condicion (ENUM)
- precio (DECIMAL > 0)
- descripcion
- especificaciones (JSON, opcional)

## ENUMs

**categoria:** COMPONENTES | REFRIGERACION | GABINETES | PERIFERICOS | MONITORES | REDES | MOBILIARIO | ALMACENAMIENTO_EXTERNO | ACCESORIOS | PORTATILES | 

**condicion:** NUEVO | COMO_NUEVO | USADO | PARA_REPUESTOS

## Flujo

- Verificar JWT con `jwt_required()`.
- Extraer `usuario_id` del JWT (nunca del body).
- Verificar estado usuario: ACTIVO (sino 403).
- Validar campos con Marshmallow.
- Verificar límite: USER_ESTANDAR máx 25 anuncios activos, TIENDA_VERIFICADA sin limites.
- INSERT con estado="ACTIVO" asignado automáticamente.

## Especificaciones JSON por categoría

```
📁 COMPONENTES
├── Procesador → socket, nucleos, velocidad_base_ghz, velocidad_boost_ghz, generacion, tdp_w, incluye_cooler
├── Placa Madre → socket, chipset, factor_forma, tipo_ram_soportada, slots_ram, puertos_m2, puertos_pcie, puertos_usb, incluye_wifi, incluye_bluetooth
├── RAM → tipo_ddr, capacidad_gb, velocidad_mhz, cantidad_modulos, latencia_cl, incluye_rgb
├── GPU → vram_gb, tipo_memoria, conectores_display, longitud_mm, tdp_w, requiere_pcie_pin, incluye_rgb
├── Almacenamiento → tipo_disco, capacidad_gb, interfaz_conexion, velocidad_lectura_mbs, velocidad_escritura_mbs, factor_forma
└── Fuente Poder → potencia_w, certificacion, modular, factor_forma, conectores_pcie

📁 REFRIGERACION
├── Aire → altura_mm, tdp_soportado_w, compatibilidad_socket, cantidad_ventiladores, incluye_rgb
├── Líquida AIO → tamano_radiador_mm, cantidad_ventiladores, compatibilidad_socket, incluye_rgb, caudal_lpm
├── Custom Loop → tipo_componente, material, diametro_tuberia_mm, compatibilidad_socket, incluye_rgb
└── Pasta Térmica → conductividad_termica_wm, capacidad_gramos, tipo_base

📁 GABINETES
├── Case → factor_forma_soportado, material, longitud_gpu_max_mm, altura_cooler_max_mm, bahias_35, bahias_25, puertos_frontales, incluye_rgb, tiene_ventana
├── Ventiladores → tamano_mm, velocidad_max_rpm, caudal_cfm, nivel_ruido_dba, conector, incluye_rgb
├── Soportes GPU → tipo_soporte, compatibilidad, material, ajustable
└── Filtros → tamano_mm, tipo_malla, lavable, posicion_case

📁 PERIFERICOS
├── Teclado → tipo_switch, factor_forma, conexion, retroiluminacion, idioma_layout, inalambrico
├── Mouse → dpi_maximo, cantidad_botones, conexion, sensor, peso_gramos, inalambrico, incluye_rgb
├── Auriculares → tipo_driver_mm, respuesta_frecuencia_hz, microfono_integrado, conexion, tipo_cancelacion_ruido, inalambrico
├── Mousepad → tamano_mm, grosor_mm, superficie, base_antideslizante, incluye_rgb
├── Micrófono → patron_polar, frecuencia_hz, conexion, tipo_montaje, incluye_filtro_pop
└── Webcam → resolucion, fps_maximo, campo_vision_grados, enfoque_automatico, incluye_microfono, conexion

📁 MONITORES
├── Monitor → resolucion, tasa_refresco_hz, tamano_pulgadas, tipo_panel, tiempo_respuesta_ms, hdr, sincronizacion_variable, puertos_entrada, curvado
├── Soportes VESA → patron_vesa_mm, peso_maximo_kg, ajuste_altura, ajuste_inclinacion, ajuste_rotacion, brazos_cantidad
└── Cables → tipo_cable, longitud_m, version_protocolo, velocidad_max_gbps

📁 REDES
├── Tarjeta Red → tipo_conexion, velocidad_maxima_mbps, bandas_wifi, estandar_wifi, interfaz_pc, incluye_antena
├── Router → velocidad_maxima_mbps, bandas, puertos_lan, estandar_wifi, mu_mimo, frecuencia_ghz
├── Adaptadores → tipo_adaptador, interfaz_entrada, interfaz_salida, velocidad_max_mbps
└── UPS → capacidad_va, potencia_w, cantidad_tomas, tiempo_respaldo_min, tipo_onda, incluye_protector_sobretension

📁 MOBILIARIO
├── Silla Gamer → peso_maximo_soportado_kg, material_tapizado, reclinable_grados, reposabrazos_ajustables, altura_ajustable, reposapiés, lumbar_ajustable
├── Escritorio → largo_cm, ancho_cm, altura_cm, material_superficie, capacidad_carga_kg, altura_ajustable, gestion_cables
└── Accesorios → tipo_accesorio, material, compatibilidad, largo_cm

📁 ALMACENAMIENTO EXTERNO
├── SSD Externo → capacidad_gb, interfaz_conexion, velocidad_lectura_mbs, velocidad_escritura_mbs, factor_forma, resistencia_agua_polvo
├── HDD Externo → capacidad_gb, interfaz_conexion, velocidad_rpm, factor_forma_pulgadas, alimentacion
├── Externos → tipo_medio, capacidad_gb, interfaz_conexion, alimentacion_requerida
└── NAS → bahias_disco, capacidad_max_tb, procesador, ram_gb, puertos_red, velocidad_red_mbps, os_incluido

📁 ACCESORIOS
├── Cables → tipo_cable, longitud_m, conectores, velocidad_transferencia_gbps, carga_maxima_w
├── Limpieza → tipo_producto, volumen_ml, superficie_aplicable, reutilizable
├── Organizadores → tipo_organizador, material, largo_cm, autoadhesivo, cantidad_piezas
└── RGB → tipo_dispositivo_rgb, conexion_control, cantidad_leds, compatible_argb, software_sincronizacion

📁 PORTATILES
├── Laptop → procesador, ram_gb, almacenamiento_gb, tamano_pantalla_pulgadas, resolucion_pantalla, gpu_dedicada, bateria_wh, peso_kg, sistema_operativo, conexiones
└── Tablet → tamano_pantalla_pulgadas, resolucion, procesador, ram_gb, almacenamiento_gb, conectividad, bateria_mah, sistema_operativo, peso_gramos, compatible_stylus
```

## Respuesta

**201 Created**
```json
{
  "id": 1,
  "titulo": "Ryzen 5 5600X casi nuevo",
  "categoria": "PROCESADOR",
  "condicion": "COMO_NUEVO",
  "precio": 450.00,
  "estado": "ACTIVO",
  "usuario_id": 1,
  "created_at": "2026-01-01T00:00:00"
}
```

## Errores

- 400 Campos obligatorios ausentes.
- 401 Sin JWT o token inválido.
- 403 Cuenta no verificada / EN_REVISION / límite 25 anuncios.
- 422 Categoría inválida / precio ≤ 0 / título > 100 chars.

## Reglas

- `usuario_id` siempre del JWT, nunca del body.
- ENUMs validados en Marshmallow antes de tocar BD.
- TIENDA_VERIFICADA sin límite de anuncios.
- Precio DECIMAL(10,2), rechazar más de 2 decimales.
- Anuncio visible en feed inmediatamente sin moderación.